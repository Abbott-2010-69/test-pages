function setup() {
createCanvas(640,480)
}

function draw() {
	/*int i,hori,vert = 0;
	const int Max_Time = 1024;
	int time[Max_Time];
	for(;i<Max_Time-1;1){
		time[i]=i;
	}
	while(hori<640){
		if(i<0)
			i=-i;
		for(w=0;w<;)
		vert=sin()
	}
		
	
	
	*/
	if (mouseIsPressed){
		fill(0);
	} else{
		fill(255);
	}
ellipse(mouseX,mouseY,20,80);
}